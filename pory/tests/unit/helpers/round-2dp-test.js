import { round2dp } from '../../../helpers/round-2dp';
import { module, test } from 'qunit';

module('Unit | Helper | round 2dp');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = round2dp(42);
  assert.ok(result);
});
